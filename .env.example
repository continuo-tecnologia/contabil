# Copy to .env (gitignored). Compose reads this file; nothing else does.
CTN_ENV=staging
COMPOSE_FILE=compose.yaml
DOMAIN=continuo.dev.br

# Laptop:
# CTN_ENV=local
# COMPOSE_FILE=compose.yaml:compose.local.yaml
# DOMAIN=localhost
